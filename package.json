{
  "name": "blissful",
  "private": true,
  "description": "Thin proxy for repo-root npm scripts. Each one delegates to apps/web-blissful (which owns the actual deps + concurrently runner).",
  "scripts": {
    "dev": "npm run --prefix apps/web-blissful dev",
    "dev:vite": "npm run --prefix apps/web-blissful dev:vite",
    "dev:shell": "npm run --prefix apps/web-blissful dev:shell",
    "build": "npm run --prefix apps/web-blissful build",
    "lint": "npm run --prefix apps/web-blissful lint"
  }
}
