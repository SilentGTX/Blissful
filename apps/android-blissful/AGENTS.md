# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# This app

`apps/android-blissful` is the React Native (react-native-tvos) rewrite of Blissful for Android TV — the current active TV effort. The full guide (stack, build/run + emulator recipe, D-pad hard rules, conventions) lives in **`DOCUMENTATION.md` in this app** — read that first. Repo-global facts + the reference-app terminology ("windows app" / "web version") live in the root `D:\JS\Blissful\CLAUDE.md`.

# Feature registry

**`docs/FEATURES.md`** (in this app) — one record per screen/feature: files, the reference it mirrors, deliberate decisions (don't "fix" those), gotchas, how to verify. Before working on a feature, read its record. After adding or changing behaviour — or discovering a decision/gotcha — update the record in the same change. Current-state only, no changelog.
