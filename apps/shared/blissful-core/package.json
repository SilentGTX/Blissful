{
  "name": "@blissful/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared pure-TS brain for Blissful: addon protocol, progress, skip, Trakt, watch-party sync, bitfield. Consumed as source by each app's bundler (Vite / Metro) via an alias — no build step. Platform-specific behavior (fetch base, storage) is injected via configureCore().",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
