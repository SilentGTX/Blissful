<?xml version="1.0" encoding="utf-8"?>
<!--
  WiX Burn bundle that wraps Blissful-Setup-<ver>.msi into a single
  Setup.exe. Users get the more-familiar "double-click Setup.exe"
  experience and Windows shows the Blissful icon on the file.

  The exe is a bootstrapper: it doesn't do the install itself, it just
  presents a UI, then chains to the embedded MSI. WiX provides the
  WixStandardBootstrapperApplication (theme = HyperlinkLicense) which
  is a built-in Aero-style wizard. No license file needed when we use
  the HyperlinkLicense variant and set LicenseUrl to a hosted page.
-->
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi"
     xmlns:bal="http://schemas.microsoft.com/wix/BalExtension">

  <Bundle Name="Blissful"
          Version="$(var.ProductVersion)"
          Manufacturer="Blissful"
          UpgradeCode="C8F4E7B3-5A92-4D11-9F8D-2E1A6B3C4D7E"
          IconSourceFile="$(var.ResourcesDir)\icon.ico"
          Compressed="yes">

    <!-- Custom theme: drops the license-link + I-agree-checkbox UI
         that the stock HyperlinkLicense theme forces, and gives the
         Blissful logo a 128x128 slot (vs the stock theme's ~64x64
         that was cropping the lotus flowers around the green B).
         theme.xml + theme.wxl live next to this file. -->
    <BootstrapperApplicationRef Id="WixStandardBootstrapperApplication.HyperlinkLicense">
      <!-- LicenseUrl is required by WiX 3.x's WixStandardBootstrapperApplication
           schema (CNDL0010), but our custom theme.xml has no EulaHyperlink
           or EulaAcceptCheckbox controls — so the URL is set internally
           and never displayed. The Install button enables without an
           acceptance gate. -->
      <bal:WixStandardBootstrapperApplication
        LicenseUrl="https://github.com/SilentGTX/Blissful"
        SuppressOptionsUI="yes"
        ThemeFile="$(var.ResourcesDir)\theme.xml"
        LocalizationFile="$(var.ResourcesDir)\theme.wxl" />
      <Payload SourceFile="$(var.ResourcesDir)\bundle-logo.png" />
    </BootstrapperApplicationRef>

    <Chain>
      <MsiPackage SourceFile="$(var.MsiPath)"
                  Vital="yes"
                  Visible="no"
                  Compressed="yes" />
    </Chain>
  </Bundle>
</Wix>
