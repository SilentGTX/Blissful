# VC++ Runtime DLLs (vcruntime140.dll, vcruntime140_1.dll, msvcp140.dll)
# are bundled at install time so blissful-shell.exe runs on machines
# without VC++ Redistributable installed. NOT committed (DLLs are copied
# at build time from %SystemRoot%\System32 — see build.ps1's staging step).
# This README is a placeholder so the directory exists in the repo.
