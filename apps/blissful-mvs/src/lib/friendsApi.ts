// Moved to @blissful/core (shared with the RN TV app). Re-exported here so the
// existing importers (`../lib/friendsApi`) don't change. The storage base is
// configured via configureCore() in the blissfulAuthApi shim.
export * from '@blissful/core/friendsApi';
