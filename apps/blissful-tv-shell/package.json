{
  "name": "blissful-tv-shell",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri v2 Android TV shell for Blissful. Hosts the apps/blissful-mvs React UI in the Android System WebView and re-implements the native bridge (player, streaming server, storage/addon proxy, update-check) for Android TV.",
  "scripts": {
    "tauri": "tauri",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build --apk",
    "android:build:aab": "tauri android build --aab",
    "desktop:dev": "tauri dev",
    "icon": "tauri icon"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
